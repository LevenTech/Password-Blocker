Private Sub Application_ItemSend(ByVal Item As Object, Cancel As Boolean)
 Dim strSubject As String
  msgBody = Item.Body
     If InStr(1, UCase(Item.Body), "PASSWORD", vbTextCompare) Then
         Prompt$ = "This email might contain a password. Are you sure you want to send it?"
       If MsgBox(Prompt$, vbYesNo + vbQuestion + vbMsgBoxSetForeground, "Possible Password Detected") = vbNo Then
        Cancel = True
      End If
    End If
End Sub